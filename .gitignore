# ---------------------------------------------------------------------- #
# 1. Dependency & Build Artifacts
# ---------------------------------------------------------------------- #
# Node.js 의존성 폴더
/node_modules

# Vite 빌드 결과물 (배포용 정적 파일)
/dist
/dist-ssr

# npm/yarn 에러 로그
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# pnpm 캐시 및 락 파일
/pnpm-lock.yaml

# Vercel/Netlify 등 배포 서비스 설정 파일 (프로젝트 특성에 따라 추가)
.vercel
.netlify

# ---------------------------------------------------------------------- #
# 2. Local Configuration & Cache
# ---------------------------------------------------------------------- #
# Vite 서버 캐시
.vite

# TypeScript 빌드 캐시 (컴파일 속도 향상용)
.tsbuildinfo

# 환경 변수 파일 (민감 정보 포함)
.env
.env.*
!.env.production  # (선택적: .env.production 파일은 포함할 수도 있음)
.env.local
.env.*.local